#include<stdio.h>
int main(){
    Printf("Aditya Bawre 10080");
    return 0;
}
/*1. Download and Install GCC Compiler:
   - Visit the official MinGW website (https://www.mingw-w64.org/).
   - Download the MinGW setup file according to your system architecture (32-bit or 64-bit).
   - Install it in your preferred directory (e.g., C:\MinGW).
   - During installation, select 'mingw32-gcc-g++' and proceed.
   - Add the bin folder path (e.g., C:\MinGW\bin) to the system environment variables under PATH.

2. Verify GCC Installation:
   - Open Command Prompt and type: gcc --version
   - If installed correctly, it will display the GCC version.

3. Install Visual Studio Code:
   - Visit https://code.visualstudio.com/ and download the setup file.
   - Run the setup and complete the installation process.
   - Open VS Code and install the 'C/C++' extension by Microsoft from the Extensions Marketplace.

4. Configure VS Code for C Programming:
   - Create a new folder for your C programs.
   - Open the folder in VS Code.
   - Create a new file with '.c' extension (e.g., hello.c).
   - Save your code and open the terminal in VS Code.

5. Write and Run the First C Program:
   - Write a simple C program (as shown below).
   - Compile it using: gcc hello.c -o hello
   - Run it using: ./hello
   */